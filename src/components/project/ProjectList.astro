---
import Image from '~/components/common/Image.astro';
import type { Project } from '~/types';

export interface Props {
  projects: Project[];
}

const { projects } = Astro.props;
---

<ul class="grid md:grid-cols-2 gap-10 mx-auto">
  {
    projects.map((project, index) => (
      <li>
        <a href={`${project.href}`}>
          <div class="">
            <Image
              src={project.img}
              alt=""
              sizes="(max-width: 800px) 100vw, 800px"
              widths={[200, 400, 800]}
              height=200
              aspectRatio="16:9"
              layout="constrained"
              loading={index <= 2 ? 'eager' : 'lazy'}
              decoding={index <= 2 ? 'sync' : 'async'}
              class="w-full rounded-md border border-neutral-200 hover:shadow-lg"
            />
            <div>
              <div class="mt-4 flex items-center">
                <h2 class="text-lg font-semibold leading-snug tracking-tight">{project.title}</h2>
              </div>

              <div class="flex gap-1 mt-1 text-gray-600">
                <p>{project.description}</p>
              </div>
            </div>
          </div>
        </a>
      </li>
    ))
  }
</ul>
